skill_id: author_attachment_monitor
tags: [monitor, attachment, safety, v2]
role: monitor_author
changes_world: false
capabilities: [artifact_write]
forbidden_capabilities: [perception_read, geometry_compute, simulation_probe, active_perception, robot_motion, gripper_control, object_manipulation]
input_ports: []
output_ports:
  - {name: monitor_program, schema: robomex.monitor_program.v1}
postconditions: [one compiler-valid fail-closed attachment monitor program is proposed]
budget: {max_turns: 3, max_model_calls: 3, max_tokens: 64000, max_wall_time_ms: 60000}
exit_conditions:
  success: "Published the compiler-validated control-hook monitor program."
  infeasible: "The graph-owned signal or debounce configuration is outside the audited contract."
functions:
  - name: build_attachment_monitor_program
    entry: "scripts/attachment_monitor.py:build_attachment_monitor_program"
    description: "Build and compile-check the exact read-only held-object loss, occlusion, and identity guard."
